---
description: Show upstream benchmark heritage and fork completion metrics
---
Report measurement honestly. The dated `benchmarks/results/` reports are inherited upstream Ponytail evidence, not fork results. Fork completion metrics and the prompt-only/deterministic/deterministic+Jev arms live under `benchmarks/completion/`. If no live fork matrix has run, say so and give the offline self-test command. Never invent a per-repo number.
